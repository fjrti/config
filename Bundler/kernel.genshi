<Bundle name="kernel" xmlns:py="http://genshi.edgewall.org/">
  <Package name='linux-virtual' />

  <!-- Never delete the currently running kernel -->
  <Package name="linux-image-${metadata.Probes['kernel'].rstrip()}" />
</Bundle>
